---
- hosts: localhost
  connection: local

  vars_files:
    - vars/main.yml
  tasks:
    - include_task: tasks/base_packages.yml
    - include_task: tasks/bash.yml
    - include_task: tasks/ssh.yml
